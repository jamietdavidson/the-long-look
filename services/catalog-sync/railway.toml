[build]
builder = "DOCKERFILE"
dockerfilePath = "services/catalog-sync/Dockerfile"
watchPatterns = [
  "services/catalog-sync/**",
  "lib/catalog-sync/**",
  "lib/http.mjs",
]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
