[build]
builder = "DOCKERFILE"
dockerfilePath = "/services/order-sync/Dockerfile"
watchPatterns = [
  "services/order-sync/**",
  "lib/order-sync/**",
]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
